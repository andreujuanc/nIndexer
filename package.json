{
  "name": "workspace",
  "version": "1.0.0",
  "description": "JS only EVM indexer",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn src/index.ts",
    "start": "npx ts-node --transpile-only src/index.ts",
    "test": "jest",
    "build": "rm -rf dist && tsc --project tsconfig.build.json"
  },
  "author": "Juan C. Andreu",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "gremlin": "^3.6.1",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "@types/gremlin": "^3.5.2",
    "@types/node": "^18.6.2",
    "@types/pg": "^8.6.5",
    "ts-jest": "^27.1.4",
    "ts-node": "^10.7.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.2"
  }
}
